# Changelog

Historical evidence is retained here rather than mixed into current installation/support claims. Earlier test totals and CI links certify only their recorded revisions, not 0.5.0.

## 0.5.1 (candidate; certification required)

- Correction discovery v3 abstains on complete, bounded notification-shaped
  envelopes rather than treating embedded report markers as direct corrections.
  Quoted envelopes preserve surrounding speech; malformed/unsupported shapes
  remain UNCERTAIN. Text never authenticates a worker or human origin.
- Original fourteen-candidate field rescan: ten notification-shaped exclusions,
  four other pending hypotheses. This is the same development snapshot, not
  independent generalization. The wider 360-row scan excludes 108 shapes and
  selects five hypotheses, including one beyond the prior short inspection window.
- All 5,562 original documents were checked against the existing retained
  kind/ID/body baseline digest; prior reviews and newer evidence remain immutable.
- No model-learning claim, live intervention deployment, component-pin upgrade,
  or modification of the immutable 0.5.0 release.

## 0.5.0

- Product renamed in place to Afterforge: “Turn agent mistakes into lasting regression checks.” Existing public repository identity/visibility preserved; canonical URL is https://github.com/ussyverse/afterforge.
- Standalone/native metadata unified at 0.5.0; afterforge, /afterforge and hermes afterforge aliases retain package/import/plugin/tool/data IDs and old commands.
- History parser v4 scopes identity by source/lineage and supports explicit exact-observation legacy reconciliation without rewriting or inventing lost evidence.
- Recipe v2 declared-v1 frozen inputs, result v3 and bundle v2 prevent same-count/hash or custom-ID false equivalence; immutable legacy readers retain their original authority.
- Capture outcomes independently bounded to 32 sessions/64 events; native operation/nonpassing verification failures return nonzero.
- Managed uv locked staged generations, hashed dependency export/resolved manifest, serialized lifecycle and atomic doctor readiness retain the prior usable runtime on failed upgrades.
- Shared guided correction/review/draft/retained-check and SQL pagination are integrated. Three packaged flows include a later reviewed commit, ignored live edits and error recovery; legacy or missing input identity cannot certify retained checks.
- Fork-safe candidate workflow and separate fail-closed stable checksum/CI locator promotion prepared. Both old and inspected v2026.9.11 host pins retained as required gates; schema 30 is not silently treated as 26.
- Current docs replace accumulated milestones. Packaged browser screenshots are retained privately; exact candidate application/native CI and local GitHub installation passed. The original prior-version fixture retained all 13 immutable document IDs/bytes, old aliases and approval history through upgrade, implementation reapproval and rollback. A pre-upgrade raw-hash discrepancy was independently proven to involve only SQLite header counters; the original baseline was not rewritten.

## Pre-0.5.0 native reminder and committed-check milestones

- Native 0.4.0 introduced an opt-in fixed project-scoped pre_verify reminder with exact-digest approval and generation-checked rollback. Synthetic hook evaluation was not model-efficacy evidence.
- Policy contract v2 bound adapter/entrypoint implementation bytes; changed/legacy approvals became inert. Repeated rollback, event-cap availability and symlink abstention were hardened.
- Passive capture, pure verification-shadow assessment and conservative captured-obligation bindings remained best-effort/shadow-only.
- Committed-current commands reused the runner with digest authorization and endpoint checks, not live-environment certification. The recorded progression reached 142 local tests before this release's additional work.
- Historical source `3b222e54e67c61ccaa07803b60a5a1f015e3168a` recorded application/native runs [34789656887](https://github.com/ussyverse/afterforge/actions/runs/34789656887) and [34789656903](https://github.com/ussyverse/afterforge/actions/runs/34789656903). Its distribution was `53edc8ac46f6af17953828c1e9a2a75e35923f77`. An isolated reminder-delivery/rollback trial was recorded privately; it was not a controlled efficacy evaluation and is not a 0.5.0 result.

## Standalone 0.2.0 / native 0.3.0 evidence milestones

- Immutable intervention proposals, frozen target/control suites, deterministic real-run receipts and explicit evidence review through CLI/web. Accept-evidence did not authorize deployment; behavioral trials remained not-run.
- Native thin adapter added eight stable tools, passive hooks, slash/terminal commands and a namespaced skill with isolated setup/removal. Runtime distribution exported committed source unchanged with RELEASE.json; no scanner patch shipped.
- First recorded distribution `4c219d2689e2fe663e16c7ae42b9fa3953de61bb` completed isolated stock-host GitHub install, registry/skill/hook dispatch, reviewed regression and teardown retaining evidence. It is historical, not the current supported pin.

## Initial standalone and continuation milestones

- Native 0.2.0 originally shipped the thin registration/runtime distribution while the standalone package remained 0.1.0; the later native 0.3.0 milestone added intervention evidence. These intentionally different historical versions are not the unified 0.5.0 metadata.
- Standalone 0.1.0 established schema-26 read-only/WAL import, immutable evidence, pinned symptom/configuration/structural adapters, bounded reviewed pytest, packaged loopback web security, pending correction/retraction review, privacy-minimized summaries and selected-source portable bundles. It did not enable automatic learning.

- Preserved initial milestone `6932ab8`; continuation `51c3c0c` hardened browser/dogfood behavior; `d495b5a` added correction candidates, portable bundles/security tests and CI. Historical application run [34781817583](https://github.com/ussyverse/afterforge/actions/runs/34781817583) passed for d495b5a with 64 local tests recorded.
- Wrapped results, delegation metadata, pagination, compaction provenance and retraction restoration received failing synthetic reproductions and fixes. Browser request generations and completion-state checks addressed stale responses rather than hiding races.
- Historical code `c1d980dd517da58b7806c83bff7d1b5b42a12dde` recorded 68 clean-checkout tests, Ruff/build, isolated wheel/skill checks, three fresh-server workflows and portable execution. These were earlier contracts, not current input-equivalence certification.
- The frozen historical cohort and private grouping erratum remain immutable: 53 cases, 20 process failures/33 controls, 52 development/one held-out control, no held-out failures or independent semantic labels. Three synthetic history-derived reductions tested assumptions, not recovered historical source. See docs/dataset-methodology.md for caveats.

No historical repository visibility statement overrides the current explicit instruction to preserve the public repository. No raw history or private local paths are included here.
