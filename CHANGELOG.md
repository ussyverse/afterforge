# Changelog

## Native plugin 0.2.0 — validation blocked

Thin native registration, eight bounded JSON tools, two metadata-only hooks, slash/terminal commands, read-only bundled skill and lazy managed-runtime adapter. Eighteen adapter tests pass alongside the 68 existing tests. Real plugin doctor passes. Full pinned installation/setup/lifecycle is blocked by the host security scanner, not certified. Manifest v1 is required by the tested installer's version ceiling; additive metadata is retained. Standalone package version remains 0.1.0.

## 0.1.0 — initial working release

- Standalone Python CLI, immutable SQLite evidence, schema-26 WAL-consistent import, bounded pagination and profile-local Hermes skill.
- Explicit adapters for Triage symptoms, Petrichor selected configuration history and shadow-only correction-aware structural records.
- Reviewed two-revision pytest execution with fixed assertions, process evidence, timeout/output handling and conservative comparison states.
- Local packaged web interface with operation states, out-of-order response guards, mutation locking, origin/token protection and safe text rendering.
- Pending human-correction candidates from role/chronology evidence, declared-review authority and reversible review history.
- Privacy-minimized summaries plus versioned selected-source portable bundles, checksums, fresh-import review and red/green execution.
- Dogfood fixes for wrapped payloads, delegation, pagination, duplicate/compacted observation retention and retraction-of-retraction behavior.
- Synthetic CI, browser workflow/security probes, package/clean-install checks and explicit historical dataset limitations.

Historical cases are not reconstructed original implementations. The three reductions use synthetic fixtures. No automatic Hermes prompt/policy/memory changes are enabled.
