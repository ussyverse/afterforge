# Afterforge 0.5.0 coordinator handoff

Exclusive release executor integrated and normally pushed candidate source `72db27e5a887379fcb4562d48f0c1cd610df9942`. Historical results remain in CHANGELOG.md. No supported pointer, version tag or GitHub release has been published for 0.5.0.

| Item | Current fact / integration action |
| --- | --- |
| Repository | Existing repository renamed to https://github.com/ussyverse/afterforge; same identity, public visibility preserved |
| Protected instructions | AGENTS.md public-visibility instruction updated with explicit user authorization; all other maintainer instructions preserved |
| Product identity | 0.5.0 metadata; Afterforge aliases with agent-fix-lab package/plugin/tool/data IDs retained |
| Implemented worker boundaries | v4 history/reconciliation; recipe v2 declared-v1, result v3, bundle v2; independent capture bounds/native exit status; uv locked staged runtime generations |
| Guided work | Shared mapping, correction discovery/review/draft/authorization/retained checks integrated; three packaged flows include later reviewed commits and error recovery |
| Docs | Current README/SPEC/architecture/data/native/recipes/interventions/validation; migration/release/contributing guides; historical milestones moved to changelog; privacy prose preserved |
| Workflow | Fork-safe candidate generation; required old/current host matrix; stable-release separately validates both exact-source CI workflows and the exact native artifact, then atomically advances supported pointer/checksum locator |
| Executor verification | 232 tests passed, 2 dependency warnings, no skips; full Ruff/format; wheel/sdist; packaged guided and expert/intervention flows each repeated three times; portable installed-CLI round trip; exact index audit of 101 text files; unchanged privacy digest |
| Current host inspection | v2026.9.11 commit 939e45c91d751fadd94dcd1b873ac3cb44846213, schema 30; actual loader/hook/schema source inspected; old pin 277268d83ff2204de9646f0b1e376e73ac0a5d20 retained |
| Candidate CI | Source 72db27e: application run 34796312216 and native run 34796312234 succeeded; exact distribution 2a20ceef8efef39bc5ebe084983260aa662884d0 installed locally from GitHub |
| Native proof | Current stock schema 30 populated with synthetic rows; 4 cases added, repeat 0; actual tools/hooks/skill/slash commands/red-green/reminder activation and rollback passed; real locked setup interruption retained old runtime and retry succeeded |
| Migration blocker | Original prior-version fixture database checksum differs from its pre-existing private baseline before executor writes; policy checksum matches. Fixture left untouched. Original consistent database/WAL snapshot or an authoritative explanation is required before exact migration certification |

## Coordinator completion gates

1. Existing-fixture baseline reconciliation and prior-version upgrade passed: independent in-memory header-counter normalization reproduced the exact original checksum, and 13 document IDs/bytes were unchanged. Old approval is preserved, requires reapproval, rejects its old digest, delivers after fresh approval and remains inert after rollback. Preserve original baseline and all evidence; repeat exact final-artifact upgrade/removal retention after any follow-up source commit.
2. Keep exact final-source application/native CI and installed-distribution evidence aligned after any follow-up commit. Run `uv run pytest -q tests packaging`; do not omit packaging tests or substitute skipped browser cases for the explicit packaged gate.
3. Audit exact staged/generated content and reachable history. docs/privacy.md remains byte-identical; the scanner still allows only its original reviewed high finding. Screenshots remain private because they contain synthetic local fixture paths.
4. Only after all gates, manually dispatch stable-release, verify locator/source/distribution/checksums, and create the separately authorized immutable 0.5.0 tag/GitHub release. No PyPI publication.

## Caveats for integration

- Both required host matrix entries passed for the recorded candidate. Follow-up commits still require their own complete runs. Stable promotion checks API/source/blob identities against the downloaded native artifact, not the moving candidate branch.
- The local current stock host reported its SQLite 3.50.4 WAL-reset vulnerability and selected DELETE journaling itself. Do not change the live host to suppress this warning; retain the stock safeguard.
- README provides concrete stable-locator/full-SHA installed alias instructions but no invented final pin. An older/missing locator is not 0.5.0 certification.
- Managed generation paths differ from the old single runtime directory; inspect final lifecycle harness expectations without editing runtime behavior merely to satisfy outdated tests.
- Model efficacy remains unmeasured. Reminder delivery, code checks, shadow receipts and evidence acceptance are separate from deployment/learning claims.

Detailed gate status and reproduction commands are in docs/validation.md. The private executor result contains final follow-up SHAs, exact header-only baseline reconciliation, record digests and migration evidence. The supported locator identifies final certified source/artifact/CI without a self-referential source commit. Private histories, resolved paths, manifests and detailed reports remain outside Git. Do not recreate the private cohort or rewrite old observations to make a result green.