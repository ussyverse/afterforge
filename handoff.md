# Afterforge 0.5.0 coordinator handoff

Current integration state, not accumulated milestones. Historical results are in CHANGELOG.md. No commit, push, supported-pointer update, tag or release was performed by the documentation/release worker.

| Item | Current fact / integration action |
| --- | --- |
| Repository | Existing repository renamed to https://github.com/ussyverse/afterforge; same identity, public visibility preserved |
| Protected instructions | AGENTS.md public-visibility instruction updated with explicit user authorization; all other maintainer instructions preserved |
| Product identity | 0.5.0 metadata; Afterforge aliases with agent-fix-lab package/plugin/tool/data IDs retained |
| Implemented worker boundaries | v4 history/reconciliation; recipe v2 declared-v1, result v3, bundle v2; independent capture bounds/native exit status; uv locked staged runtime generations |
| Guided work | Shared source mapping, correction discovery/review/draft/retained-check and SQL pagination worker is integrating; inspect final APIs before declaring release behavior complete |
| Docs | Current README/SPEC/architecture/data/native/recipes/interventions/validation; migration/release/contributing guides; historical milestones moved to changelog; privacy prose preserved |
| Workflow | Fork-safe candidate generation; required old/current host matrix; stable-release separately validates both exact-source CI workflows and the exact native artifact, then atomically advances supported pointer/checksum locator |
| Worker verification | 18 targeted release/docs tests; latest full local snapshot 231 passed, 2 warnings, no skips; full Ruff/format; wheel/sdist and installed aliases/demo red-green; three fresh installed guided servers; actionlint; temporary-index privacy audit (91 files); unchanged privacy digest |
| Current host inspection | v2026.9.11 commit 939e45c91d751fadd94dcd1b873ac3cb44846213, schema 30; actual loader/hook/schema source inspected; old pin 277268d83ff2204de9646f0b1e376e73ac0a5d20 retained |
| Release evidence | No final source/distribution SHA or CI certification exists yet; stable locator workflow is prepared, not executed |

## Coordinator completion gates

1. Integrate and review worker changes. Verify shared correction pagination/deferred linking, reviewer attribution, concrete draft versus registration, frozen input authorization and new-commit retained receipts. Preserve all immutable legacy evidence; do not silently transfer authority.
2. Run full root/native/core/test/script/example/packaging Ruff/format, all tests and builds. The reported 195-test intermediate state is not final. Release-gate tests are in packaging/test_release_gate.py and must be included via `uv run pytest -q tests packaging`.
3. Repeat final clean-wheel/browser acceptance after integration: this worker's three fresh installed guided servers passed demo/authorization/comparison and corrected-commit retention, not the entire portable/newer-commit/reminder/error UI path. Complete that broader path and capture a real synthetic-data screenshot for README. A cached executable can be selected with AFL_CHROMIUM and the wheel interpreter with AFL_BROWSER_PYTHON; keep local paths outside Git.
4. Test existing installed evidence/old aliases/reminder migration, fingerprint reapproval, no duplicate hooks, interrupted locked setup retaining old runtime and complete owned teardown. Inspect newer host schema/payload adapter changes before enabling support; a schema-26 fixture on a schema-30 host is not sufficient source-adapter evidence.
5. Audit exact staged/generated content and reachable history. docs/privacy.md remains byte-identical; scanner gate still allows only its original reviewed high finding. New findings block: do not widen allowlist, hide runtime source or remove privacy prose.
6. Only the coordinator may then commit/push. Both final exact-source application/native CI runs and exact-distribution install evidence must succeed before supported locator/pointer. Version tag/GitHub Release remains a separate authorized step after gates; no PyPI publication.

## Caveats for integration

- Current/native workflows are prepared but have not been run on GitHub for this source. The stable workflow downloads the exact successful native artifact and checks API/source/blob identities rather than promoting a moving candidate. Branch-write permissions/protection and missing artifacts fail closed.
- The new host matrix is intentionally required, not continue-on-error. Resolve current-host adapter/lifecycle mismatches instead of declaring source inspection to be a pass.
- README provides concrete stable-locator/full-SHA installed alias instructions but no invented final pin. An older/missing locator is not 0.5.0 certification.
- Managed generation paths differ from the old single runtime directory; inspect final lifecycle harness expectations without editing runtime behavior merely to satisfy outdated tests.
- Model efficacy remains unmeasured. Reminder delivery, code checks, shadow receipts and evidence acceptance are separate from deployment/learning claims.

Detailed gate status and reproduction commands are in docs/validation.md. Private histories, resolved paths, manifests and detailed reports remain outside Git. Do not recreate the private cohort or rewrite old observations to make a result green.